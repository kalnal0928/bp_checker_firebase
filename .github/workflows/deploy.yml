name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./blood-pressure-tracker

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Debug environment variables
        run: |
          if [ -n "$REACT_APP_FIREBASE_API_KEY" ]; then echo "REACT_APP_FIREBASE_API_KEY is set"; else echo "REACT_APP_FIREBASE_API_KEY is NOT set"; fi
          if [ -n "$REACT_APP_FIREBASE_AUTH_DOMAIN" ]; then echo "REACT_APP_FIREBASE_AUTH_DOMAIN is set"; else echo "REACT_APP_FIREBASE_AUTH_DOMAIN is NOT set"; fi
          if [ -n "$REACT_APP_FIREBASE_PROJECT_ID" ]; then echo "REACT_APP_FIREBASE_PROJECT_ID is set"; else echo "REACT_APP_FIREBASE_PROJECT_ID is NOT set"; fi
          if [ -n "$REACT_APP_FIREBASE_STORAGE_BUCKET" ]; then echo "REACT_APP_FIREBASE_STORAGE_BUCKET is set"; else echo "REACT_APP_FIREBASE_STORAGE_BUCKET is NOT set"; fi
          if [ -n "$REACT_APP_FIREBASE_MESSAGING_SENDER_ID" ]; then echo "REACT_APP_FIREBASE_MESSAGING_SENDER_ID is set"; else echo "REACT_APP_FIREBASE_MESSAGING_SENDER_ID is NOT set"; fi
          if [ -n "$REACT_APP_FIREBASE_APP_ID" ]; then echo "REACT_APP_FIREBASE_APP_ID is set"; else echo "REACT_APP_FIREBASE_APP_ID is NOT set"; fi
          if [ -n "$REACT_APP_FIREBASE_MEASUREMENT_ID" ]; then echo "REACT_APP_FIREBASE_MEASUREMENT_ID is set"; else echo "REACT_APP_FIREBASE_MEASUREMENT_ID is NOT set"; fi

      - name: Build project
        run: npm run build
        env:
          REACT_APP_FIREBASE_API_KEY: ${{ secrets.REACT_APP_FIREBASE_API_KEY }}
          REACT_APP_FIREBASE_AUTH_DOMAIN: ${{ secrets.REACT_APP_FIREBASE_AUTH_DOMAIN }}
          REACT_APP_FIREBASE_PROJECT_ID: ${{ secrets.REACT_APP_FIREBASE_PROJECT_ID }}
          REACT_APP_FIREBASE_STORAGE_BUCKET: ${{ secrets.REACT_APP_FIREBASE_STORAGE_BUCKET }}
          REACT_APP_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.REACT_APP_FIREBASE_MESSAGING_SENDER_ID }}
          REACT_APP_FIREBASE_APP_ID: ${{ secrets.REACT_APP_FIREBASE_APP_ID }}
          REACT_APP_FIREBASE_MEASUREMENT_ID: ${{ secrets.REACT_APP_FIREBASE_MEASUREMENT_ID }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./blood-pressure-tracker/build

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
